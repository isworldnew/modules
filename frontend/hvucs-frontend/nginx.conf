server {
    listen 5555;
    
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
    
    location /api/ {
        proxy_pass http://accident-recorder-service:8080/api/;
        proxy_set_header Host $host;
    }
    
}